#pragma once

// RETIRED stand-in. The rw::math::vpu Vector3 operation vocabulary now lives in its
// canonical RenderWare SDK home (rw/math/vpu/vector3_operation.h, sibling to types.h).
// This umbrella is kept only so existing includers keep resolving the vocabulary; new
// code should include "rw/math/vpu/vector3_operation.h" directly.
#include "rw/math/vpu/vector3_operation.h"   // rw::math::vpu operator-/operator+/Dot/Magnitude/Length/GetX/GetY/GetZ/IsValid
