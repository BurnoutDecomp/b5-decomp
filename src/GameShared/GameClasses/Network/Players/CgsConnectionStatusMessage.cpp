#include "GameShared/GameClasses/Network/Players/CgsConnectionStatusMessage.h"

// Reconstructed from BURNOUT_X360_ARTIST.XEX
//   (CgsNetwork::ConnectionStatusMessage)
//
//   GetName @ 0x827DE368:  return "Connection Status Message";
//
// GetName is the inline header-homed accessor (CgsConnectionStatusMessage.h); this
// .cpp is its definition home and pulls in the owning header so the class is the real
// ReliableMessage-derived type rather than a throwaway local shape.

namespace CgsNetwork
{
    // GetName is defined inline in the owning header (CgsConnectionStatusMessage.h);
    // nothing further to define here for this TU.
}
