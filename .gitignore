build
out
.vs
.idea

# FFmpeg compiled output (regenerated by tools/build_ffmpeg.bat; implementation in tools/build/).
# Kept here (not under build/) so it survives game-build cleans; never committed.
vendor/ffmpeg-build/

# Lua 5.1 build output (regenerated by tools/build/build_lua.bat). The source under
# vendor/lua/src is committed; the compiled lib + objects are not.
vendor/lua/obj/
vendor/lua/lua515.lib
