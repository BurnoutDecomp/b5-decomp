build
out
.vs
.idea

# FFmpeg compiled output (regenerated by tools/build_ffmpeg.bat from the vendor/FFmpeg submodule).
# Kept here (not under build/) so it survives game-build cleans; never committed.
vendor/ffmpeg-build/
